## Agent skills

### Issue tracker

Issues and PRDs live in GitHub Issues for `freshtonic/git-queue`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context — one `CONTEXT.md` + `docs/adr/` at the repo root (created lazily by `/domain-modeling`). See `docs/agents/domain.md`.
